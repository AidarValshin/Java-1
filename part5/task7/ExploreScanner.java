package main.ru.mephi.java.part5.task7;

import java.util.Scanner;

public class ExploreScanner {

    Scanner in = new Scanner(System.in);
   /*
    278  public Scanner(Path source) throws IOException {
        this(Files.newInputStream(source));
    }

584  public void close() {
        if (!this.closed) {
            if (this.source instanceof Closeable) {
                try {
                    ((Closeable)this.source).close();
                } catch (IOException var2) {
                    this.lastException = var2;
                }
            }

            this.sourceClosed = true;
            this.source = null;
            this.closed = true;
        }
    }


    */
}
